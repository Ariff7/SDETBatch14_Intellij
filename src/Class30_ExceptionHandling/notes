Exception :
    -Exceptions are unwanted behaviours which occurs during runtime and can terminate programs or can change the
    normal behavior of a program

Why should we handle exceptions : ?
   -When we handle exceptions it leads to a very good user experience
   -(For technical guys) - we can read the error messages and can figure out what we are doing wrong
   -When we handle the exceptions properly,omly the problematic code will not execute , the rest of the code will still
   continue to work.

How to Handle the exceptions : ?



 Exception handling :
     is the process of responding to unwanted or unexpected events when a computer program runs.

Most important for your framework:
classes and objects
methods
inheritance
loops
if else
String class
Collection framework(List and Map)
Arrays
file handling
======================
verbal interview Questions
all OOPS


